# Sample cloudflared tunnel config. The app generates the real one at runtime
# under ~/.local/share/remoteDesktop/ — this file is only a format reference.
tunnel: example-tunnel
credentials-file: ~/.cloudflared/<tunnel-id>.json

ingress:
  - hostname: remotedesktop.example.com
    service: http://localhost:10000
  - service: http_status:404
